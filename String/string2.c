#include<stdio.h>
        int main(){
        char* name="Lucknow";
        char city[]="Kanpur";
        /* int len =-1;
        while(name[++len]!='\0');*/
        printf("length=%ld\n",strlen(name));
        return 0;
}
